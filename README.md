# ag

Small core: an MCP task queue of probes. A probe is trusted only if it first
fails a counterexample, then passes on the real tree. Exit codes are the chain.
Everything AI writes is untrusted until those codes exist.
